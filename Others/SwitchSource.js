// Unit: SwitchSource

function SwitchSource(FromLocation, FromSource, ToLocation, ToSource)
{
    if (FromLocation == ToLocation)
    {
        if (FromSource == ToSource)
        {
            return true;
        }
    }
}